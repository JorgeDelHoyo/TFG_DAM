package com.example.tfgv01.ui.theme

import androidx.compose.ui.graphics.Color

// ══════════════════════════════════════════════════════════
// 🎸 ARPEG.IO — Paleta Lila Pastel / Suave
//    Basada en el logo oficial (lavanda suave)
//    Tonos desaturados para una vista agradable y relajada.
// ══════════════════════════════════════════════════════════

// ── Primarios (lila suave, pastel) ───────────────────────
val ArpegPrimary           = Color(0xFFCBB8E0) // Lavanda pastel (suave)
val ArpegPrimaryDark       = Color(0xFFAE97C8) // Lila medio-suave
val ArpegPrimaryContainer  = Color(0xFF3D2E52) // Violeta oscuro apagado (contenedores dark)
val ArpegOnPrimaryContainer= Color(0xFFF0EAF5) // Lila muy claro sobre contenedor

// ── Secundarios (complementario suave) ───────────────────
val ArpegSecondary          = Color(0xFF9A85B0) // Violeta grisáceo suave
val ArpegSecondaryContainer = Color(0xFF2D2240) // Violeta oscuro desaturado
val ArpegOnSecondaryContainer = Color(0xFFE0D6EC) // Lila claro para texto

// ── Terciarios (acento rosa suave) ───────────────────────
val ArpegTertiary           = Color(0xFFD4B0DC) // Rosa-lila pastel
val ArpegTertiaryContainer  = Color(0xFF4E2D56) // Magenta apagado
val ArpegOnTertiaryContainer= Color(0xFFF5EDF7) // Rosa blanquecino

// ── Fondos (modo oscuro con matiz violeta MUY sutil) ─────
val ArpegDarkBackground   = Color(0xFF121017) // Negro con leve tinte violeta
val ArpegDarkSurface      = Color(0xFF1C1924) // Superficie oscura suave
val ArpegDarkSurfaceHigh  = Color(0xFF262230) // Superficie elevada

// ── Fondos (modo claro) ──────────────────────────────────
val ArpegLightBackground  = Color(0xFFF8F4FC) // Blanco lavanda muy suave
val ArpegLightSurface     = Color(0xFFFFFBFF) // Blanco con toque mínimo

// ── Texto ────────────────────────────────────────────────
val ArpegOnDarkSurface    = Color(0xFFF0EAF5) // Texto principal en oscuro (lila blanquecino)
val ArpegOnLightSurface   = Color(0xFF201A28) // Texto principal en claro
val ArpegOnPrimary        = Color.White       // Texto sobre botones primarios

// ── Error ────────────────────────────────────────────────
val ArpegError            = Color(0xFFCF6679) // Rojo rosado suave
val ArpegOnError          = Color.Black

// ── Extras ───────────────────────────────────────────────
val ArpegOutline          = Color(0xFFA898B8) // Bordes sutiles lila grisáceo
val ArpegOutlineDark      = Color(0xFF4A4258) // Bordes en modo oscuro